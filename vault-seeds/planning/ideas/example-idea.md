---
title: Example idea - delete once you have real ones
state: not-started
horizon: inbox
area: software
projects: []
domains: []
created: 2026-08-14
cssclasses: [state-not-started]
---

This is a seed note showing the floor schema in use. `title` and `state` are the only
required fields; everything else here is optional and shown for illustration.

Write as little or as much as the idea earns - a sentence is a complete idea note. Link
related ideas with `[[wikilinks]]` instead of nesting folders; use `projects`/`domains` for
soft overlap across more than one thing at once.

Delete this note (or move it to `../archive/`) once the corpus has real content.
