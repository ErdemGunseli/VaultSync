# ideas/

The corpus. One note per idea; body scales with importance (one sentence -> pages +
diagrams). Overlap via `projects`/`domains` tags and `[[wikilinks]]`, never subfolders. See
`../_templates/idea.md` for the floor schema and `example-idea.md` for a worked note -
delete the example once real ideas accrete.
