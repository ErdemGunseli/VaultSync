# inbox/

Raw captures land here (GTD: capture -> clarify). One idea per note; a sentence is fine.
Triage from here into `../ideas/` - set `horizon`, `area`, `projects`, `domains`, links.
An agent may *suggest* the triage; you give the soft go (nothing files itself silently).
